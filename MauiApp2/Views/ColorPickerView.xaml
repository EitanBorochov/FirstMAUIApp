<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp2.Views.ColorPickerView">
    <Frame VerticalOptions="Center" HorizontalOptions="Center" 
           BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray900}}"
           CornerRadius="18" Padding="5" HasShadow="True"
           HeightRequest="400" WidthRequest="320">
        <VerticalStackLayout Spacing="20">
                <Label Text="Choose Color" FontSize="32" TextColor="{AppThemeBinding
                Light={StaticResource PrimaryTextColor}, Dark={StaticResource PrimaryDarkText}}"/>
            
                <CollectionView
                    x:Name="ColorPickerButtons"
                    Margin="10"
                    SelectionMode="None"
                    ItemsSource="{Binding ColorPalette}">
                    
                    <CollectionView.ItemsLayout>
                        <GridItemsLayout Orientation="Horizontal" HorizontalItemSpacing="5" Span="1">
                        </GridItemsLayout>
                    </CollectionView.ItemsLayout>

                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Button Clicked="OnColorTapped"
                                    BackgroundColor="{Binding .}"
                                    HeightRequest="40"
                                    WidthRequest="40"
                                    CornerRadius="20" />
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
    
                </CollectionView>
        </VerticalStackLayout>
        
    </Frame>
</ContentView>